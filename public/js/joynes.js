joynes = {
  Base : {},

  Master : function(nes, socket) {
    this.initialize(nes, socket);
    return this;
  },

  Slave : function(nes, socket) {
    this.initialize(nes, socket);
    return this;
  }
};
